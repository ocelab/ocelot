package it.unisa.ocelot.genetic.encoding.graph;

import it.unisa.ocelot.c.types.CType;

public class StructNode extends Node {

    public StructNode(int pId, CType pType) {
        super(pId, pType);
    }
}
