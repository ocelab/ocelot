package it.unisa.ocelot.genetic.encoding.graph;

import it.unisa.ocelot.c.types.CType;

public class PointerNode extends Node {
    public PointerNode(int pId, CType pType) {
        super(pId, pType);
    }
}
